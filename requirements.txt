# Core tools have zero dependencies: new / index / validate / type_tables / backlinks / dashboard
# Only Python 3 standard library is used, works out-of-the-box in any environment with python3.
#
# The following are installed as needed:
duckdb>=0.10        # tools/index.py / tools/query.py — SQL query layer (skips data.duckdb if not installed)
biopython>=1.80     # tools/gb_annotate.py — parse .gb plasmid maps to auto-fill cards
python-pptx>=0.6    # tools/ppt2md.py     — convert PPT to AI-readable markdown
markitdown          # tools/lit_from_pdf.py — PDF → text extraction (optional, zo-pdf-to-markdown also works)
#
# Install (optional):  pip install -r requirements.txt
