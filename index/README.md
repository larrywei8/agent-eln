# eln/index/

Auto-generated by `python tools/index.py`. Contains:

- `records.csv`, per-type CSVs — flat views over all records
- `graph.json` — provenance graph
- `data.duckdb` — queryable DuckDB
- `dashboard.html` — click to open

Everything here is regeneratable from the source Markdown and is Gitignored by default.

- **Canonical:** Markdown records under `eln/`, `lims/`, `methods/`, and `wiki/`.
- **Shareable derived views:** CSV, `graph.json`, and `dashboard.html`; regenerate before sharing.
- **Machine-local caches:** `.cache.json` and `data.duckdb`; never treat them as source data.
